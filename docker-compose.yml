services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_HOST_AUTH_METHOD: "trust"
    volumes:
      - ./tmp/db:/var/lib/postgresql/data:delegated

  vite:
    build: .
    platform: linux/amd64
    command: sh -c "npm ci && bin/vite dev"
    tty: true
    environment:
      VITE_RUBY_HOST: 0.0.0.0
    ports:
      - "3391:3391"
    volumes:
      - .:/opt/rails:delegated
      - bundle_cache:/usr/local/bundle:delegated

  web:
    build: .
    platform: linux/amd64
    command: sh -c "rm -f tmp/pids/server.pid && bundle exec rails s -b0"
    environment:
      - SECRET_KEY_BASE
      - VITE_RUBY_HOST=vite
    volumes:
      - .:/opt/rails:delegated
      - bundle_cache:/usr/local/bundle:delegated
    ports:
      - "3299:3000"
    healthcheck:
      disable: true
    depends_on:
      - db
      - redis
      - memcached
      - vite

  redis:
    image: redis:alpine

  memcached:
    image: memcached:1.6-alpine

volumes:
  bundle_cache:
