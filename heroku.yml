build:
  docker:
    web: Dockerfile.production
  config:
    RAILS_ENV: production
release:
  image: web
  command:
    - ./deployment-tasks.sh
run:
  web: bundle exec rails s -b0
  cron:
    command:
      - bundle exec clock
    image: web
